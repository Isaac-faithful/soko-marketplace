const $=s=>document.querySelector(s),safe=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);let orders=[];
async function api(url,options={}){const r=await fetch(url,{...options,headers:{"Content-Type":"application/json",...(options.headers||{})}}),d=await r.json();if(r.status===401||r.status===403){location.href="auth.html";throw Error("Buyer sign-in required")}if(!r.ok)throw Error(d.error);return d}
async function load(){orders=await api("/api/buyer/orders");render()}
function render(){$("#orders").innerHTML=orders.length?orders.map(o=>`<article class="order"><header class="order-head"><div><strong>Order ${o.id}</strong><small>${new Date(o.created_at+"Z").toLocaleDateString()} · ${safe(o.store_name)}</small></div><strong>${o.currency} ${Number(o.total).toLocaleString()}</strong></header><div class="order-body"><div><small>ORDER STATUS</small><br><span class="status">${safe(o.status)}</span></div><div><small>DELIVERY</small><br><strong>${safe(o.destination)}</strong>${o.carrier?`<small>${safe(o.carrier)} · ${safe(o.tracking_number)}</small>${o.estimated_delivery?`<small>Estimated ${new Date(o.estimated_delivery+"T12:00:00").toLocaleDateString()}</small>`:""}${o.tracking_url?`<a href="${safe(o.tracking_url)}" target="_blank" rel="noopener">Track with carrier ↗</a>`:""}`:""}</div><div>${o.dispute_status!=="none"?`<span class="dispute">Dispute: ${safe(o.dispute_status.replaceAll("_"," "))}</span>`:`<button class="help" data-dispute="${o.id}">Get help with order</button>`}</div></div>${o.tracking_events?.length?`<div class="order-body"><div><small>TRACKING TIMELINE</small>${o.tracking_events.map(e=>`<p><strong>${safe(e.message)}</strong><br><small>${new Date(e.created_at+"Z").toLocaleString()}</small></p>`).join("")}</div></div>`:""}</article>`).join(""):'<div class="empty"><strong>No orders yet</strong><p>Products you purchase from database merchants will appear here.</p></div>';document.querySelectorAll("[data-dispute]").forEach(b=>b.addEventListener("click",()=>openDispute(b.dataset.dispute)))}
function openDispute(id){$("#modalContent").innerHTML=`<span class="kicker">SOKO SUPPORT</span><h2>Get help with ${id}</h2><p>Opening a dispute asks the Soko Trust team to review the buyer, merchant and order records.</p><form id="disputeForm"><textarea name="reason" required minlength="10" placeholder="Tell us what went wrong"></textarea><button class="primary">Open dispute →</button></form>`;$("#backdrop").classList.add("open");$("#disputeForm").addEventListener("submit",async e=>{e.preventDefault();try{await api(`/api/buyer/orders/${id}/dispute`,{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});close();await load();toast("Dispute opened for Soko review")}catch(err){toast(err.message)}})}
function close(){$("#backdrop").classList.remove("open")}let timer;function toast(t){$("#toast").textContent=t;$("#toast").classList.add("show");clearTimeout(timer);timer=setTimeout(()=>$("#toast").classList.remove("show"),2200)}$("#close").addEventListener("click",close);$("#backdrop").addEventListener("click",e=>{if(e.target===$("#backdrop"))close()});$("#logout").addEventListener("click",async()=>{await api("/api/auth/logout",{method:"POST"});location.href="auth.html"});load().catch(()=>{});
