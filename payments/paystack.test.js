const assert=require("node:assert/strict");const crypto=require("node:crypto");const {PaystackProvider}=require("./paystack");
async function run(){const calls=[],fakeFetch=async(url,options)=>{calls.push({url,options});return{ok:true,status:200,json:async()=>({status:true,data:{authorization_url:"https://checkout.paystack.test/demo",reference:"SOKO-NGN-TEST"}})}};const p=new PaystackProvider({secretKey:"sk_test_example",fetchImpl:fakeFetch});const initialized=await p.initializePayment({email:"buyer@example.com",amount:18000,currency:"NGN",reference:"SOKO-NGN-TEST",metadata:{orderId:"SK1"}});assert.equal(initialized.reference,"SOKO-NGN-TEST");const sent=JSON.parse(calls[0].options.body);assert.equal(sent.amount,"1800000");assert.equal(sent.currency,"NGN");assert.deepEqual(sent.channels,["bank_transfer","bank","ussd","card"]);const raw=JSON.stringify({event:"charge.success",data:{reference:"SOKO-NGN-TEST"}}),sig=crypto.createHmac("sha512","sk_test_example").update(raw).digest("hex");assert.equal(p.verifyWebhook(raw,sig),true);assert.equal(p.verifyWebhook(raw,"bad"),false);assert.equal(p.fromSubunit(1800000),18000);console.log("Paystack adapter tests passed")}
run().catch(e=>{console.error(e);process.exitCode=1});
