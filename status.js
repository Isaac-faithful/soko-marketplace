const status=document.querySelector("#status");const safe=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);async function load(){const me=await fetch("/api/me");if(!me.ok){location.href="auth.html";return}const user=await me.json();if(user.user.role!=="admin"){location.href="/";return}const r=await fetch("/api/health"),h=await r.json();status.innerHTML=`<div class="status-row"><span>Service</span><strong class="ok">${h.ok?"Healthy":"Unavailable"}</strong></div><div class="status-row"><span>Environment</span><strong>${safe(h.environment)}</strong></div><div class="status-row"><span>Email provider</span><strong>${safe(h.emailProvider)}</strong></div><div class="status-row"><span>Payment mode</span><strong class="${h.paymentsProvider==="development"?"demo":"ok"}">${safe(h.paymentsProvider)}</strong></div><div class="status-row"><span>Scheduled backups</span><strong class="${h.backupConfigured?"ok":"demo"}">${h.backupConfigured?"Configured":"Not configured"}</strong></div><div class="status-row"><span>Off-site backups</span><strong class="${h.offsiteBackupConfigured?"ok":"demo"}">${h.offsiteBackupConfigured?"Configured":"Not configured"}</strong></div><div class="status-row"><span>Checked at</span><strong>${new Date().toLocaleString()}</strong></div>`}load().catch(()=>{status.textContent="Unable to load system status."});
